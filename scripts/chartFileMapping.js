/**
 * Export object key must match with the chart name in library
 * Each chart should include all the classes in its directory to generate a complete doc
 */

module.exports = {
  ColumnChart: [
    '../src/lib/Column.chart.js',
    '../src/lib/XYChart.js',
    '../src/lib/ChartInterface.js',
  ],
};
